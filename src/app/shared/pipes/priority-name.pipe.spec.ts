import { PriorityNamePipe } from './priority-name.pipe';

describe('PriorityNamePipe', () => {
  it('create an instance', () => {
    const pipe = new PriorityNamePipe();
    expect(pipe).toBeTruthy();
  });
});
